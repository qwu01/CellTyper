# write a sampler to make a meaningful split of training/test dataset.
    # Make sure that the CellType labels are relatively balanced.
    # Make sure that the Method annotations are relatively balanced.